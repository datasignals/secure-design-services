# frontend-deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.registration.deployment.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.registration.deployment.replicas }}
  selector:
    matchLabels:
      app: cms-registration
  template:
    metadata:
      labels:
        app: cms-registration
        cluster-autoscaler.kubernetes.io/safe-to-evict: 'false'
    spec:
      volumes:
        - name: frontend-config
          secret:
            secretName: {{ .Values.secrets.frontendConfig.name }}               
      containers:
        - name: cms-frontend
          image: '{{ .Values.registration.image.repository }}:{{ .Values.registration.image.tag }}'
          ports:
            - name: http
              containerPort: {{ .Values.registration.deployment.containerPort }}
              protocol: TCP
          volumeMounts:
            - name: frontend-config
              mountPath: {{ .Values.secrets.frontendConfig.mountPath }}
              subPath: {{ .Values.secrets.frontendConfig.subPath }} 

          resources: {}
      restartPolicy: Always
      securityContext: {}
      imagePullSecrets:
        - name: {{ .Values.secrets.dockerLockular.name }}
      schedulerName: {{ .Values.frontend.deployment.schedulerName }}

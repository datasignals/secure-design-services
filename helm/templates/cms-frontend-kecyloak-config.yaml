# Server Config Secret
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secrets.keycloakConfig.name }}
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  config.json: {{ .Files.Get .Values.secrets.keycloakConfig.filePath | b64enc | quote }}
